import { classPrefixer } from '../../../helpers/class-prefixer';
import { module, test } from 'qunit';

module('Unit | Helper | class prefixer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = classPrefixer(42);
  assert.ok(result);
});
